#pragma once

class poolManager
{
public:
			poolManager();
			~poolManager();
			/*static poolManager *createPoolManager(int size)
			{
						auto poolM = new (std::nothrow) poolManager;
						poolM->_size = size;
						return poolM;
			}

			void addObject(std::string objType,_type object)
			{
						if (_poolQuene.size() < _size)
						{
									for_each(_poolQuene.begin())
						}
						else
						{

						}
			}

			void clearObject()
			{
						if (!(_poolQuene.empty()))
						{
									return;
						}
						else
						{
									_poolQuene.clear();
						}
			}

			_type getObject(std::string objType)
			{

			}


private:
			std::map<std::string,std::list<_type>> _poolQuene;
			int _size;*/
};

